// Entity and response types are defined once, in the shared package, so the
// clients are typed by exactly what this API returns.
export type { FoodEntry, ExerciseEntry, WeightEntry, DaySummary, ApiResponse } from "@nutrition/shared";
