# Copy this to .env.local for local development
# Copy this to .env.production for production build

# Backend API URL
# Local development (uses Vite proxy, so /api works without a full URL)
VITE_API_URL=

# Production — set this to your actual API domain
# VITE_API_URL=https://api.yourdomain.com
